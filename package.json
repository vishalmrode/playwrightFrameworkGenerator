{
  "name": "playwright-framework-generator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:local": "vite build --base=/",
    "generate:manual:pdf": "node ./scripts/generate_manual_pdf.js",
    "type-check": "tsc",
    "preview": "vite preview",
    "preview:local": "npm run build:local && vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint ."
  }
}
