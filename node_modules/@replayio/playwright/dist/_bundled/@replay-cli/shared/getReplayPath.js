'use strict';

var os = require('os');
var path = require('path');

function getReplayPath(...path$1) {
  let basePath;
  if (process.env.RECORD_REPLAY_DIRECTORY) {
    basePath = process.env.RECORD_REPLAY_DIRECTORY;
  } else {
    basePath = path.join(os.homedir(), ".replay");
  }
  return path.resolve(path.join(basePath, ...path$1));
}

exports.getReplayPath = getReplayPath;
