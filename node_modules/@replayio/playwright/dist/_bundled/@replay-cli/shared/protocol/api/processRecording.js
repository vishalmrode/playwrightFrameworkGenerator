'use strict';

async function processRecording(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Recording.processRecording",
    params
  });
}

exports.processRecording = processRecording;
