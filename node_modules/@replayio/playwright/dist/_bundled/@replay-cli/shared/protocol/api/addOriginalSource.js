'use strict';

async function addOriginalSource(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Recording.addOriginalSource",
    params
  });
}

exports.addOriginalSource = addOriginalSource;
