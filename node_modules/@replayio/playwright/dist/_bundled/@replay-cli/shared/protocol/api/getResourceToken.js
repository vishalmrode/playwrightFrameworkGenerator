'use strict';

async function getResourceToken(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Resource.token",
    params
  });
}

exports.getResourceToken = getResourceToken;
