'use strict';

async function checkIfResourceExists(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Resource.exists",
    params
  });
}

exports.checkIfResourceExists = checkIfResourceExists;
