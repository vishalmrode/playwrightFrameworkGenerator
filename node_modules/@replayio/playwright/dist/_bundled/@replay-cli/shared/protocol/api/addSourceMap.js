'use strict';

async function addSourceMap(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Recording.addSourceMap",
    params
  });
}

exports.addSourceMap = addSourceMap;
