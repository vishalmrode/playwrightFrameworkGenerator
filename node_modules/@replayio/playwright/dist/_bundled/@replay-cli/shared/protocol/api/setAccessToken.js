'use strict';

async function setAccessToken(client, params) {
  return await client.sendCommand({
    method: "Authentication.setAccessToken",
    params
  });
}

exports.setAccessToken = setAccessToken;
