'use strict';

async function reportCrash(client, params) {
  await client.waitUntilAuthenticated();
  return await client.sendCommand({
    method: "Internal.reportCrash",
    params
  });
}

exports.reportCrash = reportCrash;
