'use strict';

var exitTasks = require('./exitTasks.js');

async function waitForExitTasks() {
  await Promise.all(exitTasks.exitTasks.map((task) => task()));
}

exports.waitForExitTasks = waitForExitTasks;
