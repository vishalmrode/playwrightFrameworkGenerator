'use strict';

var path = require('path');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var path__default = /*#__PURE__*/_interopDefault(path);

function generateDefaultTitle(metadata) {
  let host = metadata.uri;
  if (host && typeof host === "string") {
    try {
      const url = new URL(host);
      host = url.host;
    } finally {
      return `Replay of ${host}`;
    }
  }
  if (Array.isArray(metadata.argv) && typeof metadata.argv[0] === "string") {
    return `Replay of ${path__default.default.basename(metadata.argv[0])}`;
  }
}

exports.generateDefaultTitle = generateDefaultTitle;
